name: aicr_cli
description: AI Code Reviewer (AICR) - A command-line tool for analyzing Flutter + BLoC code changes with deterministic rules and optional AI-assisted review.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.1

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  crypto: ^3.0.3
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  test: ^1.25.0
  lints: ^4.0.0
